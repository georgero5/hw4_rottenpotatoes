%h1 Find Movies with same director

%table#movies
  %thead
    %tr
      %th= link_to 'Movie Title', :id => 'title_header'
      %th Rating
      %th= link_to 'Release Date', :id => 'release_date_header'
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)